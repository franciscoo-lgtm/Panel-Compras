'use client'
import type { DetailProp } from '../EmbarqueDetailClient'
export function ResumenTab(_: { detail: DetailProp }) {
  return <p className="text-zinc-500 text-[12px]">Resumen — pendiente.</p>
}
